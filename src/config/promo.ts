/**
 * Конфигурация промо-акций (pop-up окно).
 *
 * Как пользоваться — см. src/components/landing/promo/README.md
 *
 * Главное правило: при запуске НОВОЙ акции — поменяйте `id`.
 * Это сбросит localStorage-флаг "уже видел" для всех посетителей.
 */

export interface PromoConfig {
  /** Глобальный включатель модуля. false → pop-up не рендерится вообще. */
  enabled: boolean;

  /**
   * Уникальный ID акции (slug).
   * Используется как ключ localStorage и как form_id в аналитике.
   * При смене ID все пользователи увидят pop-up заново.
   */
  id: string;

  /** Через сколько миллисекунд после загрузки страницы показать окно (обычный посетитель). */
  delayMs: number;

  /** Через сколько мс показать окно посетителю с utm_campaign содержащим "exhibition". */
  exhibitionDelayMs: number;

  /** Контент окна. */
  content: {
    /** Маленький бейдж над заголовком, напр. "Акция" */
    badge?: string;
    /** Главный заголовок (поддерживает HTML внутри <strong>/<span>) */
    title: string;
    /** Подзаголовок / описание */
    subtitle: string;
    /** Список буллетов (необязательно) */
    bullets?: string[];
    /** Текст на основной CTA-кнопке, напр. "Воспользоваться" */
    ctaLabel: string;
    /** Мелкий текст под кнопкой (например условия, дедлайн) */
    footnote?: string;
  };

  /**
   * Аналитика. section/buttonLabel прокидываются в форму заявки,
   * которая открывается после клика на CTA.
   */
  analytics: {
    section: string;     // напр. "promo_popup"
    buttonLabel: string; // дублирует ctaLabel, но можно переопределить
  };
}

export const PROMO_CONFIG: PromoConfig = {
  enabled: true,
  id: "promo-2026-holiday-design-1",
  delayMs: 15_000,
  exhibitionDelayMs: 0,

  content: {
    badge: "Подарок от компании",
    title: 'Индивидуальный дизайн платформы <span class="text-emerald-500">к вашему празднику</span>',
    subtitle: "Ваш подарок — индивидуальный дизайн платформы «За!Подарком» к одному любому календарному празднику на выбор! Выберите главную дату, и мы сделаем для неё неповторимое оформление.",
    bullets: [
      "Любой календарный праздник на ваш выбор",
      "Уникальное оформление специально для вас",
      "Быстрое согласование и запуск",
    ],
    ctaLabel: "Выбрать праздник",
    footnote: "Оставьте заявку — наш менеджер свяжется с вами",
  },

  analytics: {
    section: "promo_popup",
    buttonLabel: "Выбрать праздник (промо)",
  },
};
